patch                ::= "PATCH" "(" patch_construct_body ")"
patch_construct_body ::= /* define the patch construct body rules */
                         identifier "," identifier /* current proposed rule replaces first identifier with second */